# 服务名称
spring:
  application:
    name: beacon-monitor

  # 多环境
  profiles:
    active: dev
  # nacos注册中心地址
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.88.128:8848
      # nacos配置中心地址:
      config:
        server-addr: 192.168.88.128:8848
        file-extension: yml
        # beacon-api-dev.yml

  mail:
    host: smtp.qq.com
    port: 465
    username: 2931163626@qq.com
    password: optzmoheptapdcfb
    tos: lc204573@gmail.com
    protocol: smtp
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            enable: true # 开启SSL
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory

cache:
  client:
    auth:
      enabled: true
      caller: beacon-monitor
      secret: cache_monitor_secret
